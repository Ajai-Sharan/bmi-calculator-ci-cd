apiVersion: apps/v1
kind: Deployment
metadata:
  name: bmi-calculator-api
  labels:
    app: bmi-calculator-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bmi-calculator-api
  template:
    metadata:
      labels:
        app: bmi-calculator-api
    spec:
      containers:
      - name: bmi-calculator-api
        image: docker.io/<DOCKERHUB_USERNAME>/bmi-calculator-api:latest
        ports:
        - containerPort: 3000
        env:
        - name: PORT
          value: "3000"

        # ------------------------
        # Health Checks
        # ------------------------
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10

        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5

        # ------------------------
        # Resource Management
        # ------------------------
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"

---
apiVersion: v1
kind: Service
metadata:
  name: bmi-calculator-api-service
spec:
  type: LoadBalancer
  selector:
    app: bmi-calculator-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000



